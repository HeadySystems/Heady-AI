{
  "name": "@heady/contracts",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Heady contract surface — OpenAPI 3.1 source of truth + mcp-tools.json generator. Types/Zod derive via codegen (ADR-0001).",
  "main": "src/index.mjs",
  "exports": {
    ".": "./src/index.mjs",
    "./openapi": "./openapi/heady.openapi.json"
  },
  "scripts": {
    "build": "echo \"(no build — runnable ESM; Kubb/openapi-ts codegen wired when deps install)\"",
    "test": "node --test",
    "lint": "echo \"(lint wired at repo level)\""
  },
  "keywords": ["heady", "contracts", "openapi", "mcp-tools", "zod", "codegen"]
}
