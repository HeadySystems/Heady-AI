{
  "name": "@heady/embedding",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Heady embedding pipeline — durable workflow + instantaneous-acquisition ruleset (ADR-0024).",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./core": "./src/core.mjs"
  },
  "scripts": {
    "test": "node --test test/core.test.mjs test/corpus.test.mjs",
    "lint": "echo \"(lint wired at repo level)\"",
    "build": "echo \"(tsc build wired at repo level)\""
  },
  "keywords": ["heady", "embedding", "pgvector", "vectorize", "workflow"]
}
