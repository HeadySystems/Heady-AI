{
  "name": "@heady/secrets",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fail-closed secret loading via secure injection (GCP Secret Manager / env) + heady-secrets rotate CLI.",
  "main": "src/index.mjs",
  "bin": {
    "heady-secrets": "src/cli.mjs"
  },
  "exports": {
    ".": "./src/index.mjs",
    "./registry": "./src/registry.mjs",
    "./core": "./src/core.mjs"
  },
  "scripts": {
    "secrets": "node src/cli.mjs",
    "doctor": "node src/cli.mjs doctor",
    "test": "node --test test/secrets.test.mjs",
    "lint": "echo \"(lint wired at repo level)\"",
    "build": "echo \"(no build — runnable ESM)\""
  },
  "keywords": ["heady", "secrets", "rotation", "gcp", "secret-manager", "vault", "security"]
}
