---
name: heady-replan
description: "Use when implementing dynamic re-planning on roadblocks, adaptive agent behavior, or automatic plan revision in the Heady™ ecosystem. Keywords include replan, re-planning, roadblock, adaptive, plan revision, dynamic planning, recovery, retry strategy."
---

# heady-replan

> This is a Workflow Wrapper exposing the `heady-replan` Skill to the UI command palette.

## Trigger Details
When this workflow is executed via the `/heady-replan` slash command, the system will automatically invoke the underlying skill and execute its designated logic.

Refer to the primary definition in `.agents/skills/heady-replan/SKILL.md` for full operational details.
