---
name: heady-multi-model
description: "Heady™ cross-provider AI routing — access Claude, GPT-4o, Gemini, Groq, and HeadyBuddy through a unified interface with model-selection guidance."
---

# heady-multi-model

> This is a Workflow Wrapper exposing the `heady-multi-model` Skill to the UI command palette.

## Trigger Details
When this workflow is executed via the `/heady-multi-model` slash command, the system will automatically invoke the underlying skill and execute its designated logic.

Refer to the primary definition in `.agents/skills/heady-multi-model/SKILL.md` for full operational details.
