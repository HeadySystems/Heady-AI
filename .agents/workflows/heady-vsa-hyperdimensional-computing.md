---
name: heady-vsa-hyperdimensional-computing
description: "Apply Heady™-style vector-symbolic architecture for state machines, associative memory, hypervector binding, bundling, permutation, and similarity-based retrieval. Use when the user mentions VSA, hyperdimensional computing, tensor-native state logic, codebook retrieval, or replacing branch-heavy logic with vector operations."
---

# heady-vsa-hyperdimensional-computing

> This is a Workflow Wrapper exposing the `heady-vsa-hyperdimensional-computing` Skill to the UI command palette.

## Trigger Details
When this workflow is executed via the `/heady-vsa-hyperdimensional-computing` slash command, the system will automatically invoke the underlying skill and execute its designated logic.

Refer to the primary definition in `.agents/skills/heady-vsa-hyperdimensional-computing/SKILL.md` for full operational details.
