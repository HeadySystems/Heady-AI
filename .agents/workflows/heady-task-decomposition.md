---
name: heady-task-decomposition
description: ">"
---

# heady-task-decomposition

> This is a Workflow Wrapper exposing the `heady-task-decomposition` Skill to the UI command palette.

## Trigger Details
When this workflow is executed via the `/heady-task-decomposition` slash command, the system will automatically invoke the underlying skill and execute its designated logic.

Refer to the primary definition in `.agents/skills/heady-task-decomposition/SKILL.md` for full operational details.
