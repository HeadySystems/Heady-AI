---
name: heady-semantic-backpressure
description: ">"
---

# heady-semantic-backpressure

> This is a Workflow Wrapper exposing the `heady-semantic-backpressure` Skill to the UI command palette.

## Trigger Details
When this workflow is executed via the `/heady-semantic-backpressure` slash command, the system will automatically invoke the underlying skill and execute its designated logic.

Refer to the primary definition in `.agents/skills/heady-semantic-backpressure/SKILL.md` for full operational details.
