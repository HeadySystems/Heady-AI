---
name: heady-merkle-index
description: "Use when implementing incremental codebase indexing, file change detection, or efficient re-embedding pipelines in the Heady™ ecosystem. Keywords include Merkle tree, incremental indexing, file hashing, change detection, embedding pipeline, re-index, sync."
---

# heady-merkle-index

> This is a Workflow Wrapper exposing the `heady-merkle-index` Skill to the UI command palette.

## Trigger Details
When this workflow is executed via the `/heady-merkle-index` slash command, the system will automatically invoke the underlying skill and execute its designated logic.

Refer to the primary definition in `.agents/skills/heady-merkle-index/SKILL.md` for full operational details.
