---
name: heady-dependency-guard
description: "Use when monitoring dependency trees for CVEs, generating automated version bump PRs, and ensuring dependency health across all 78 repositories. Keywords include dependency, CVE, security vulnerability, version bump, ppnpm audit, supply chain, Snyk, Dependabot."
---

# heady-dependency-guard

> This is a Workflow Wrapper exposing the `heady-dependency-guard` Skill to the UI command palette.

## Trigger Details
When this workflow is executed via the `/heady-dependency-guard` slash command, the system will automatically invoke the underlying skill and execute its designated logic.

Refer to the primary definition in `.agents/skills/heady-dependency-guard/SKILL.md` for full operational details.
