---
name: heady-cloud-orchestrator
description: "Use when orchestrating cloud resources, managing XET storage, automating git commits, or handling remote compute in the Heady™ ecosystem. Keywords include cloud orchestrator, XET storage, auto-commit, remote compute, cloud resources, multi-cloud, resource orchestration, and compute management."
---

# heady-cloud-orchestrator

> This is a Workflow Wrapper exposing the `heady-cloud-orchestrator` Skill to the UI command palette.

## Trigger Details
When this workflow is executed via the `/heady-cloud-orchestrator` slash command, the system will automatically invoke the underlying skill and execute its designated logic.

Refer to the primary definition in `.agents/skills/heady-cloud-orchestrator/SKILL.md` for full operational details.
