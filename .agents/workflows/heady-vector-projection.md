---
name: heady-vector-projection
description: "Use when working with the vector projection engine, vector serving, vector pipelines, or 3D spatial computing in the Heady™ ecosystem. Keywords include vector projection, vector serve, vector pipeline, 3D spatial, projection engine, vector template, spatial computing, and vector operations."
---

# heady-vector-projection

> This is a Workflow Wrapper exposing the `heady-vector-projection` Skill to the UI command palette.

## Trigger Details
When this workflow is executed via the `/heady-vector-projection` slash command, the system will automatically invoke the underlying skill and execute its designated logic.

Refer to the primary definition in `.agents/skills/heady-vector-projection/SKILL.md` for full operational details.
