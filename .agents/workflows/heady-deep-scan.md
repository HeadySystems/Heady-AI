---
name: heady-deep-scan
description: "Project-wide context mapping via Heady™DeepScan — maps an entire workspace into 3D vector memory for deep understanding before any major work."
---

# heady-deep-scan

> This is a Workflow Wrapper exposing the `heady-deep-scan` Skill to the UI command palette.

## Trigger Details
When this workflow is executed via the `/heady-deep-scan` slash command, the system will automatically invoke the underlying skill and execute its designated logic.

Refer to the primary definition in `.agents/skills/heady-deep-scan/SKILL.md` for full operational details.
