---
name: heady-swarm-template-ops
description: "Use when managing the HeadySwarm dynamic creation lifecycle — template matching, provisioning, parallel execution, convergence, deployment, and template extraction. Implements §33"
---

# heady-swarm-template-ops

> This is a Workflow Wrapper exposing the `heady-swarm-template-ops` Skill to the UI command palette.

## Trigger Details
When this workflow is executed via the `/heady-swarm-template-ops` slash command, the system will automatically invoke the underlying skill and execute its designated logic.

Refer to the primary definition in `.agents/skills/heady-swarm-template-ops/SKILL.md` for full operational details.
