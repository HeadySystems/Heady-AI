# HeadySystems Platform

> Projection vertical for **headysystems.com**

Built from `latent-core-dev` Hub → projected to `headysystems-production` spoke via GitHub Actions.

## Build

```bash
npm run build --workspace=apps/headysystems
```
