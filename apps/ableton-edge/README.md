# Ableton Edge Node

> Projection vertical for **Ableton SysEx Edge** — Cloud MIDI Sequencer

Built from `latent-core-dev` Hub → projected to `ableton-edge-production` spoke via GitHub Actions.

## Build

```bash
npm run build --workspace=apps/ableton-edge
```
