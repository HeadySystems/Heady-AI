# HeadyMCP Dashboard

> Projection vertical for **headymcp.com**

Built from `latent-core-dev` Hub → projected to `headymcp-production` spoke via GitHub Actions.

## Build

```bash
npm run build --workspace=apps/mcp-dashboard
```

## Stack

- Webpack Module Federation
- Sacred Geometry UI Kit
- Real-time SSE telemetry
