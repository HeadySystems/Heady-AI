name: Deploy Heady Buddy API

on:
  push:
    branches: [main]
    paths:
      - 'services/heady-buddy-api/**'
  workflow_dispatch:
  workflow_call:

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: us-central1
  SERVICE: heady-buddy-api

jobs:
  build-and-deploy:
    name: 'Build & Deploy Buddy API'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v3

      - name: Deploy to Cloud Run
        working-directory: services/heady-buddy-api
        run: |
          cat > /tmp/env.yaml << 'ENVEOF'
          NODE_ENV: production
          HEADY_VERSION: "${{ github.sha }}"
          HEADY_SERVICE: heady-buddy-api
          SENTRY_DSN: "https://9367535bcdaffa7f2e2c3edb4edded79@o4510998791192576.ingest.us.sentry.io/4511070397464576"
          CORS_ORIGINS: "https://headyme.com,https://headysystems.com,https://headyconnection.org,https://headybuddy.org,https://headymcp.com,https://headyio.com,https://headybot.com,https://headyapi.com,https://heady-ai.com"
          BUDDY_API_URL: "https://heady-buddy-api-bf4q4zywhq-uc.a.run.app"
          AUTH_SERVER_URL: "https://heady-auth-server-bf4q4zywhq-uc.a.run.app"
          ENVEOF
          gcloud run deploy $SERVICE \
            --source . \
            --region $REGION \
            --min-instances=0 \
            --max-instances=8 \
            --memory=512Mi \
            --cpu=1 \
            --port=3002 \
            --concurrency=100 \
            --timeout=60s \
            --allow-unauthenticated \
            --env-vars-file=/tmp/env.yaml

      - name: Get service URL
        run: |
          URL=$(gcloud run services describe $SERVICE --region=$REGION --format='value(status.url)')
          echo "Buddy API deployed to: $URL"
