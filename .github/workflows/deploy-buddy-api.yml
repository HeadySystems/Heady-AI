name: Deploy Heady Buddy API

on:
  push:
    branches: [main]
    paths:
      - 'services/heady-buddy-api/**'
  workflow_dispatch:
  workflow_call:

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: us-central1
  SERVICE: heady-buddy-api

jobs:
  build-and-deploy:
    name: 'Build & Deploy Buddy API'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v3

      - name: Deploy to Cloud Run (source deploy)
        working-directory: services/heady-buddy-api
        run: |
          gcloud run deploy $SERVICE \
            --source . \
            --region $REGION \
            --min-instances=0 \
            --max-instances=8 \
            --memory=512Mi \
            --cpu=1 \
            --port=3002 \
            --concurrency=100 \
            --timeout=60s \
            --allow-unauthenticated \
            --set-env-vars=NODE_ENV=production \
            --set-env-vars=HEADY_VERSION=${{ github.sha }} \
            --set-env-vars=PORT=3002 \
            --set-env-vars="^||^CORS_ORIGINS=https://headyme.com||https://headysystems.com||https://headyconnection.org||https://headybuddy.org||https://headymcp.com||https://headyio.com||https://headybot.com||https://headyapi.com||https://headyai.com"

      - name: Get service URL
        run: |
          URL=$(gcloud run services describe $SERVICE --region=$REGION --format='value(status.url)')
          echo "Buddy API deployed to: $URL"
