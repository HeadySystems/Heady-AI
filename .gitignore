
*~
audit_logs.jsonl
*.backup-*
# Backup files
*.bak
*.bak.*
build/
# Build artifacts
.cache/
!configs/nginx/ssl/*.crt
!configs/nginx/ssl/*.key
!configs/nginx/ssl/*.pem
/coverage/
coverage/
**/*-credentials.json
**/credentials.json
*.crt
/data/
data/
dist/
.DS_Store
.env
.env.*
!.env.example
# Environment
.env.local
.env.production
# Heady .gitignore — Clean root, no artifacts in repo
.heady-private/
# ── Heady Security & Ops Ignores (auto-generated) ──
# IDE
.idea/
*.jsonl
*.key
*.local
# Lock files (using npm — pnpm lock should not coexist)
*.log
# Node
node_modules/
npm-debug.log*
# OS
*.pem
*.pid
# pnpm-lock.yaml
# Private architecture docs
# Runtime artifacts
# Runtime data (written continuously by HCFP pipeline)
# Runtime / ephemeral state
**/*secret*
*.secret
# Secrets prevention — never commit these patterns
!**/self-awareness.js
server.pid
**/service-account.json
*.swo
*.swp
*.temp
# Temp files
test.txt
*.tgz
Thumbs.db
*.tmp
/tmp/
logs/
