# Secrets — never commit (AGENTS.md §Do Not). Use .env.example as the template.
.env
.env.*
!.env.example

# Dependencies / build
node_modules/
dist/
.turbo/

# Derived, reconstructible artifacts (ADR-0000 — never authoritative). Regenerable from the SoT:
#   .data/vector-memory   → `heady-embed`
#   .data/decomposition   → `node tooling/decomposition/src/decompose.mjs`
#   .data/coherence       → `node tooling/coherence/src/coherence.mjs all`
.data/

# Generated bundles — reconstructible, not committed to the clean repo
*.zip
