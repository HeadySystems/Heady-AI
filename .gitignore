# Secrets — never commit (AGENTS.md §Do Not). Use .env.example as the template.
.env
.env.*
!.env.example

# Dependencies / build
node_modules/
dist/
.turbo/

# Derived, reconstructible vector-memory projections (ADR-0000) — regenerable via `heady-embed`.
.data/vector-memory/
