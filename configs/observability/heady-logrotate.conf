# HeadySwarm local-development log rotation only.
# Cloud Run emits structured stdout and does not persist or rotate local files.
/home/headyme/.pm2/logs/*.log {
    daily
    missingok
    rotate 7
    compress
    delaycompress
    notifempty
    copytruncate
    maxsize 10M
    dateext
}
